// Variáveis que aparecem somente no "console.log"

let idade = 5;
console.log(idade);
let altura = 180;
console.log(altura);

// Letra Maiuscula e Minuscula não utilizar... caso seja NomeCompleto = Incorreto. O certo é minuscula e maiuscula... nomeCompleto = Correto
// verificar corretamente as variavéis ao digitar no "console.log"

let nome = 'Marcelo';
console.log(nome)